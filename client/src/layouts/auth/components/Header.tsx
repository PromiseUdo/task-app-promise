import Logo from "@/common/logo";

const Header = () => {
  return (
    <header className="w-full px-4 overlay py-[26px] sm:px-8 sm:py-10 lg:px-10 lg:py-6 h-full flex justify-start items-start bg-expectoo-shades-white ">
      <Logo />
    </header>
  );
};

export default Header;
